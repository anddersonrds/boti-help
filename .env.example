# Gemini API key. Without it the app still boots and runs on the mock classifier.
GEMINI_API_KEY=

# Set to 1 to force the mock classifier and never call the model.
MOCK_LLM=0

# Port of the Express process that serves the API and, in production, the web build.
PORT=3000
